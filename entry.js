const {join} = require("path");const base = join(__dirname, "src/clients/brainlab/sunsetter/SS006");const testInfo={id:'SS006',site:'sunsetter',title:'4.2 Get pricing_1 step full form + no skip + awning pricing blurred',client:'brainlab',target:'https://www.sunsetter.com/cm/customer-information/',variation:'1'};module.exports={js:join(base,"index.ts"),css:join(base,"styles/main.scss"),testInfo: testInfo};module.exports.logActiveTestInfo=()=>{console.log("Running test info: ",JSON.stringify(testInfo))};